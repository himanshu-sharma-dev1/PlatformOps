apiVersion: v1
kind: Service
metadata:
  name: {{ include "platformops.fullname" . }}
  labels:
    {{- include "platformops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "platformops.selectorLabels" . | nindent 4 }}
